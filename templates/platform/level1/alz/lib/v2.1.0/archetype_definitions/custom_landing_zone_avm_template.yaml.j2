---
# AVM alz-lib format archetype definition for custom landing zone: {{ mg_custom.archetype_definitions[item].archetype_id }}
# Generated by Ansible from custom_landing_zones.caf.platform.yaml
# Used by the alz Terraform provider (Azure/alz) via library_references custom_url.
name: "{{ mg_custom.archetype_definitions[item].archetype_id }}"
{% if mg.archetype_definitions[item].policy_assignments is defined and mg.archetype_definitions[item].policy_assignments is mapping %}
policy_assignments:
{% for key in mg.archetype_definitions[item].policy_assignments.keys() %}
  - "{{ key }}"
{% endfor %}
{% endif %}
{% if mg_custom.archetype_definitions[item].policy_assignments is defined and mg_custom.archetype_definitions[item].policy_assignments is mapping %}
{% if mg.archetype_definitions[item].policy_assignments is not defined or mg.archetype_definitions[item].policy_assignments is not mapping %}
policy_assignments:
{% endif %}
{% for key in mg_custom.archetype_definitions[item].policy_assignments.keys() %}
  - "{{ key }}"
{% endfor %}
{% endif %}
{% if mg_custom.archetype_definitions[item].policy_definitions is defined and mg_custom.archetype_definitions[item].policy_definitions is mapping %}
policy_definitions:
{% for key in mg_custom.archetype_definitions[item].policy_definitions.keys() %}
  - "{{ key }}"
{% endfor %}
{% endif %}
{% if mg_custom.archetype_definitions[item].policy_set_definitions is defined and mg_custom.archetype_definitions[item].policy_set_definitions is mapping %}
policy_set_definitions:
{% for key in mg_custom.archetype_definitions[item].policy_set_definitions.keys() %}
  - "{{ key }}"
{% endfor %}
{% endif %}
{% if mg_custom.archetype_definitions[item].role_definitions is defined and mg_custom.archetype_definitions[item].role_definitions is mapping %}
role_definitions:
{% for key in mg_custom.archetype_definitions[item].role_definitions.keys() %}
  - "{{ key }}"
{% endfor %}
{% endif %}
